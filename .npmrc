# Necesario para instalar librerías de JSR (jsr.io) con npm.
# @pinta365/coros vive en JSR, no en el registry de npm: npm.jsr.io es el
# espejo compatible con npm (es el que escribe `npx jsr add`).
@jsr:registry=https://npm.jsr.io
